(ns senior-project-profiles-server.core-test
  (:require [clojure.test :refer :all]
            [senior-project-profiles-server.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
